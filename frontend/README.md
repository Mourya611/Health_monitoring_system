# Frontend

React + Vite dashboard for:
- Realtime Firebase sensor visualization
- Patient profile input (`age`, `gender`, `weight`, `height`)
- Risk prediction display from backend API (`High Risk` / `Low Risk`)
- Sliding-window dataset preview and CSV export

Run from this folder:

```powershell
npm install
npm run dev
```
